package com.example.slmns.ksustudyroom;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.EditText;
import android.widget.TextView;

/**
 * Test Activity feel free to delete this Activity and activity_test.xml. Free free to remove the "To Test Page" links in the layout views as well.
 */
public class TestActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_test);
       // final EditText testUserName = (EditText) findViewById(R.id.loginUsernameText);
       // final EditText testPassword = (EditText) findViewById(R.id.loginPasswordText);
       // final TextView welcomeMessage = (TextView) findViewById(R.id.testWelcomeMessage);
       // welcomeMessage.setText("hi" +RegisterActivity.);
    }
}
